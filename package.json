{
	"name": "ft-intro-rpg",
	"workspaces": [
		"client",
		"server"
	],
	"scripts": {
		"dev": "concurrently -n api,site -c blue,green \"npm run dev --prefix server\" \"wait-on tcp:localhost:3001 && npm run dev --prefix client\"",
		"build": "concurrently -n site,api -c green,blue \"npm run build --prefix client\" \"npm run build --prefix server\""
	},
	"devDependencies": {
		"concurrently": "^9.2.0",
		"wait-on": "^9.0.1"
	}
}
